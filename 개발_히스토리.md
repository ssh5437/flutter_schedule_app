# Flutter 스케줄 앱 개발 히스토리

## 프로젝트 개요
현장 서비스 스케줄 관리 앱 - 에어컨 청소, 세탁기 청소 등 현장 서비스 일정 관리

## 주요 개발 내용

### 1. 초기 문제 해결 - 데이터 저장 이슈
**문제**: 스케줄 추가 시 저장이 안 되어 목록과 캘린더에 표시되지 않음

**원인**:
- `schedule_form_screen.dart`에서 `DatabaseHelper`로 저장
- `home_screen.dart`에서 `StorageHelper`로 읽기
- `calendar_screen.dart`에서 `DatabaseHelper`로 읽기
- 저장소가 통일되지 않음

**해결**:
- 모든 화면을 `DatabaseHelper`로 통일
- `main.dart`에서 스케줄 추가 후 홈 화면과 캘린더 자동 새로고침 구현

### 2. 캘린더 화면 개선

#### 2-1. 기본 캘린더 커스터마이징
- 날짜 셀 내부에 스케줄 정보 표시
- 방문 시간, 작업 내용, 작업 건수 한 줄로 표시
- 상태별 색상 구분 (확정: 녹색, 예정: 주황색, 완료: 회색, 취소: 빨간색)

#### 2-2. 화면 크기 최적화
- 세로보기: 화면 세로로 꽉 차게 표시
- 가로보기: RotatedBox로 90도 회전, 화면 가로로 꽉 차게 표시
- 상단에 세로/가로 전환 버튼 추가

#### 2-3. 동적 행 높이 적용
- 각 주(행)마다 스케줄 개수에 따라 높이 자동 조절
- 스케줄 0~1개: 기본 높이 80px
- 스케줄 2개 이상: 날짜 표시 50px + (스케줄 개수 × 35px)
- 예: 6개 스케줄 = 260px (50 + 210)
- SingleChildScrollView로 스크롤 가능하게 변경

#### 2-4. 스케줄 정렬 및 표시 개선
- 방문 시간 없는 스케줄이 먼저 표시
- 방문 시간 있는 스케줄은 시간순으로 정렬
- 요청일자만 있어도 캘린더에 표시 (visitDate || requestDate)
- 방문 시간 있을 때만 시간 표시, 없으면 시간 미표시

#### 2-5. 커스텀 캘린더 구현
- TableCalendar 대신 직접 구현
- `_buildCalendarHeader()`: 헤더 (년월, 좌우 화살표)
- `_buildDaysOfWeekRow()`: 요일 행
- `_buildCalendarRows()`: 동적 높이의 날짜 셀들
- `_buildDayCell()`: 개별 날짜 셀 (스케줄 목록 포함)

### 3. 스케줄 추가 페이지 최적화

#### 3-1. 레이아웃 개선
- Column + Expanded + SingleChildScrollView 구조
- 스케줄 추가 버튼을 하단에 고정 (항상 보임)
- 모든 여백 축소 (16 → 12, 필드 간격 16 → 8)
- `isDense: true`, contentPadding 축소로 컴팩트하게

#### 3-2. 필드 배치 최적화
- 방문확정일자 + 방문시간을 한 행에 배치
- 작업 건수 단독 배치 (상태 필드 제거됨)
- 주소 필드 한 줄로 변경
- 비고 필드 한 줄로 변경

#### 3-3. 방문시간 셀렉트박스 변경
**이전**: TimePicker 다이얼로그
**변경 후**: DropdownButtonFormField
- 5:00 ~ 23:00, 30분 단위 (05:00, 05:30, 06:00, ...)
- "미정" 옵션 포함
- 레이블: "방문확정시간" → "방문시간"

#### 3-4. 작업 건수 및 상태 자동화
- 작업 건수 기본값: 1
- 상태 필드 제거 (사용자 선택 불가)
- 자동 상태 설정 로직:
  - 방문일자 + 방문시간 모두 있음 → "확정"
  - 하나라도 없음 → "예정"

#### 3-5. 작업 내용 개별 건수 입력
**이전**: 작업 항목만 선택 (체크박스)
**변경 후**: 각 작업 항목마다 개별 건수 지정

**새로운 다이얼로그**:
- 미리 정의된 항목: + 버튼으로 추가
- 추가된 항목: -/+ 버튼으로 건수 증감
- 건수가 1일 때 - 버튼 누르면 항목 제거
- 직접 입력 항목: 🗑️ 삭제 버튼으로 완전 제거

**데이터 저장 형식**:
- workItems: ["벽걸이 에어컨 1건", "냉장고 2건"]
- workCount: 3 (총 합계)

**총 작업 건수 필드**:
- 읽기 전용 (회색 배경)
- 작업 항목 선택 시 자동 계산

### 4. 스케줄 추가 후 자동 새로고침

**구현**:
```dart
// main.dart
final GlobalKey<HomeScreenState> _homeKey = GlobalKey<HomeScreenState>();
final GlobalKey<CalendarScreenState> _calendarKey = GlobalKey<CalendarScreenState>();

if (result != null) {
  _homeKey.currentState?.refresh();
  _calendarKey.currentState?.refresh();
  setState(() {});
}
```

**결과**: 스케줄 추가/수정 시 목록과 캘린더 모두 즉시 업데이트

## 현재 앱 구조

### 화면 구성
1. **스케줄 목록** (HomeScreen): 예정/확정 상태 스케줄 목록
2. **캘린더** (CalendarScreen): 월간 캘린더 + 날짜별 스케줄
3. **완료 내역** (CompletedSchedulesScreen): 완료된 스케줄

### 데이터 모델
```dart
class Schedule {
  int? id;
  String customerName;      // 고객명
  DateTime requestDate;     // 요청일자
  DateTime? visitDate;      // 방문확정일자
  String? visitTime;        // 방문시간 (HH:mm)
  String phoneNumber;       // 전화번호
  String address;          // 주소
  String? companyName;     // 업체명
  List<String> workItems;  // 작업 내용 (예: ["벽걸이 에어컨 1건", "냉장고 2건"])
  int workCount;          // 총 작업 건수
  String? notes;          // 비고
  String status;          // 상태 (예정/확정/완료/취소)
}
```

### 작업 항목 (미리 정의된 항목)
- 1way 에어컨
- 2way 에어컨
- 스텐드 에어컨
- 벽걸이 에어컨
- 드럼세탁기
- 통돌이세탁기
- 냉장고
- 입주 청소
- 이사 청소
- 정기 청소

### 방문시간 옵션
- 05:00 ~ 23:00 (30분 단위)
- 총 37개 시간 옵션 + "미정"

## 주요 기능

### 스케줄 관리
- ✅ 스케줄 추가/수정/삭제
- ✅ 작업 항목별 개별 건수 지정
- ✅ 자동 상태 관리 (예정/확정)
- ✅ 총 작업 건수 자동 계산

### 캘린더
- ✅ 동적 행 높이 (스케줄 개수에 따라)
- ✅ 날짜 셀에 스케줄 상세 정보 표시
- ✅ 세로/가로 보기 전환
- ✅ 스크롤 가능한 월간 뷰
- ✅ 요청일자 기반 표시

### UI/UX
- ✅ 컴팩트한 입력 폼
- ✅ 스케줄 추가 버튼 하단 고정
- ✅ 셀렉트박스 방식 시간 선택
- ✅ 실시간 총 건수 계산

## 기술 스택
- Flutter SDK: ^3.9.2
- sqflite: 로컬 데이터베이스
- table_calendar: 캘린더 UI
- intl: 날짜 포맷팅 (한국어)

## 다음 개선 사항 (제안)
- [ ] 스케줄 검색 기능 강화
- [ ] 스케줄 필터링 (날짜 범위, 상태별)
- [ ] 통계 및 리포트 기능
- [ ] 스케줄 알림 기능
- [ ] 고객 정보 관리
- [ ] 데이터 백업/복원

---
생성일: 2025-10-05
